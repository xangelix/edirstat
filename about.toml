accepted = [
    "Apache-2.0",
    "MIT",
    "BSD-2-Clause",
    "BSD-3-Clause",
    "ISC",
    "BSL-1.0",
    "OFL-1.1",
    "Ubuntu-font-1.0",
    "Zlib",
    "CC0-1.0",
    "Unicode-3.0",
    "CDLA-Permissive-2.0",
    "MIT-0",
    "MPL-2.0",
]

# Dependency filtering
ignore-build-dependencies = true
ignore-dev-dependencies = true
ignore-transitive-dependencies = false

# Limit directory traversal to crate root to avoid non-deterministic ties
# caused by readdir order across filesystems (ext4 vs btrfs vs overlayfs)
max-depth = 1

